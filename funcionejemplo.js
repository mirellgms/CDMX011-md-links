const nombreFuncion = (nombre) => `Que onda no me gusta el hallowin ${nombre}`

const errorFuncion = (nombre) => `Nel Estas mal bro jaja ${nombre}`

module.exports = {
    nombreFuncion,
    errorFuncion
}